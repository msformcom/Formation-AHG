<Window x:Class="SimpleWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SimpleWPF"
        xmlns:vm="clr-namespace:SimpleWPF.ViewModels.Pages"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:AccueilVM}" >
            <TextBlock Text="{Binding Message}"></TextBlock>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MaxHeight="200"></RowDefinition>
            <RowDefinition  Height="*"></RowDefinition>
            <RowDefinition  Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" MaxWidth="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <local:Entete Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"></local:Entete>
        <local:Menu x:Name="Menu" MenuKey="MainMenu"  Grid.Row="1" Grid.Column="0"></local:Menu>
        <ContentControl Grid.Column="1" Grid.Row="1" Content="{Binding CurrentVM, ElementName=Menu}"></ContentControl>
    </Grid>
</Window>
